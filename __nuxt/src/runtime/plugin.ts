import { defineNuxtPlugin } from 'nuxt/app';

export default defineNuxtPlugin((_nuxtApp) => {
    // console.log('Plugin injected by my-module!');
    // This is probably where live preview post messaging goes?
});
